# Relative URL → Vite dev proxy to Spring Boot (same-origin; auth cookies work on HTTP).
# MAIN/baseline worktree: frontend runs on :3002 and proxies to the main backend on :8002.
VITE_API_BASE_URL="/api/v1"
VITE_BACKEND_ORIGIN="http://localhost:8002"
